public static class Enums
{
    public enum PlayerState
    {
        Move,
        Jump,
        Hit
    }

    public enum SFXClipType
    {
        UIClick,
        GameStart,
        GameEnd,
        PlayerJump,
        PlayerLand,
        PlayerHit,
        Count
    }
}

